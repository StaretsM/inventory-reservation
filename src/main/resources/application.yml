spring:
  flyway:
    enabled: true
    locations: classpath:db/migration

  application:
    name: inventory-reservation

  jpa:
    hibernate:
      ddl-auto: validate
    generate-ddl: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  reservation:
    expiration-minutes: 10